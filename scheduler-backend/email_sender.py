# input: all interview details as individual strings
# output: True if emails sent successfully, False if failed
def send_confirmation(
    candidate_name: str,
    candidate_email: str,
    interviewer_name: str,
    interviewer_email: str,
    interview_time: str,
    interview_date: str
) -> bool:
    pass